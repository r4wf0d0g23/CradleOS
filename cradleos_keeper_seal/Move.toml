[package]
name = "cradleos_keeper_seal"
edition = "2024"

# No dependency on the world package — keeper_seal is pure CradleOS-side
# content. Identity binding uses raw Sui addresses (character.character_address)
# so we don't import world::character::Character. Avoids upgrade coupling.

[environments]
testnet_stillness = "4c78adac"
